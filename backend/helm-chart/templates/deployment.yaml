apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "my-django-app.fullname" . }}
  labels:
    app: {{ include "my-django-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "my-django-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "my-django-app.name" . }}
    spec:
      containers:
        - name: django-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.resources | nindent 12 }}